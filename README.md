DriverClass is the main class in which all other classes objects are created and also it displays menu.
Custom Exceptions are being implemented and if user enter any invalid input exceptions are thrown.
